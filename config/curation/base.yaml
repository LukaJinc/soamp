paths:
  cache_dir: .cache
  data_dir: data
  reports_dir: reports

crawl:
  max_workers: 20
  max_id: 24207
  id_margin: 500
  request_timeout_s: 20

split:
  identity_threshold: 0.60
  test_size: 0.2
  random_seed: 42
  post_filtering: true
